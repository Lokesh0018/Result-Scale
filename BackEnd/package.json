{
  "name": "backend",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development ts-node-dev --respawn --transpile-only src/server.ts",
    "dev:railway": "cross-env NODE_ENV=development ts-node-dev --respawn --transpile-only src/railwayServer.ts",
    "dev:render": "cross-env NODE_ENV=development ts-node-dev --respawn --transpile-only src/renderServer.ts",
    "build": "tsc",
    "start": "cross-env NODE_ENV=production node dist/server.js",
    "start:railway": "cross-env NODE_ENV=production node dist/railwayServer.js",
    "start:render": "cross-env NODE_ENV=production node dist/renderServer.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@getbrevo/brevo": "^5.0.4",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongodb": "^7.2.0",
    "mongoose": "^9.6.2",
    "nodemailer": "^8.0.7"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.8.0",
    "@types/nodemailer": "^8.0.0",
    "cross-env": "^10.1.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.3"
  }
}
